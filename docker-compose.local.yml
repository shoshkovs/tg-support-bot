# Совместимость: `docker compose -f docker-compose.local.yml up -d` эквивалентен `docker compose up -d`.
# Локальный HTTP: docker/nginx/conf.d/00-local-http.conf
include:
    - path: docker-compose.yml
